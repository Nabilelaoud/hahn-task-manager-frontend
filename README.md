# Hahn Task Manager – Frontend

React frontend for the Hahn Task Manager internship test.  
This application consumes the Spring Boot backend API and provides a responsive UI for managing projects and tasks.

To log in and test the application, please use the following credentials:

- Email: `test@hahn.com`
- Password: `password123`

## Tech Stack

- **Framework**: React (Create React App)
- **Language**: JavaScript (ES6+)
- **Styling**: CSS (custom styles, responsive layout)
- **HTTP Client**: Fetch API / Axios
- **State Management**: React hooks (`useState`, `useEffect`, etc.)

## Getting Started

### Prerequisites

Make sure the following tools are installed:

- Node.js (LTS version)
- npm (comes with Node.js)
- Git

### Installation

Clone the repository and install dependencies:

### Running the app

1. Start the development server:
   npm start

2. The app will be available at:

- `http://localhost:3000`

Make sure the backend is running (by default on `http://localhost:8080`) so that the frontend can communicate with the API.
